{
  "name": "v50-tier3-team-23",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "build": "npx if-env TYPE=server && (cd server && npm install) || (cd frontend && npm install && npm run build)",
    "server": "node ./server/server.js",
    "frontend": "cd frontend && npm run dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "if-env": "^1.0.4"
  }
}
